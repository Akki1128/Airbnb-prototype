# ===== Backend (Node + Express) =====
NODE_ENV=development
PORT=8000

# MySQL (backend DB)
DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASS=yourpassword
DB_NAME=airbnb_clone

# Session secret & CORS
SESSION_SECRET=your_session_secret
CORS_ORIGIN=http://localhost:5173

# ===== Frontend (React) =====
REACT_APP_TRAVELER_API=http://localhost:8000
REACT_APP_OWNER_API=http://localhost:8001
REACT_APP_AUTH_MODE=cookie

# ===== Agent (FastAPI) =====
AGENT_PORT=8500
TAVILY_API_KEY=your_tavily_key

# Web / Auth (adjust if your app expects these) for both backend and traveler
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=devsecret_traveler
SSO_JWT_SECRET=dev_sso_secret_change_me